# ES6 Promises

This project is part of the Holberton School / Holberton curriculum, focused on learning ES6 Promises in JavaScript.

## Learning Objectives

At the end of this project, you should be able to explain:

- Promises (how, why, and what)
- How to use the `then`, `resolve`, `catch` methods
- How to use every method of the Promise object
- Throw / Try
- The await operator
- How to use an async function

## Requirements

- All files interpreted/compiled on Ubuntu 18.04 LTS using NodeJS 12.11.x
- Allowed editors: vi, vim, emacs, Visual Studio Code
- All files should end with a new line
- A `README.md` file at the root of the project folder is mandatory
- Code should use the `.js` extension

## Tasks

### 0. Keep every promise you make and only make promises you can keep

File: `0-promise.js`

Returns a Promise using the prototype `function getResponseFromAPI()`.